---
- hosts: localhost
  become: True

  vars:
    - debug: yes
    - user_name: user_test
    - user_group: "{{ user_name }}"
    - user_home: "/home/{{ user_name }}"
    - user_ssh_pub_file: "/etc/ansible/roles/role_under_test/tests/files/{{ user_name }}.pub"

  roles:
    - role_under_test